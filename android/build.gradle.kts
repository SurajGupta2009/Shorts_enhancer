// Top-level build file. Versions are pinned deliberately: this project builds with
// AGP 8.5.2 + Kotlin 1.9.24 + Gradle 8.7 + JDK 17, which is a combination that is
// widely mirrored and therefore fast and reliable to resolve in CI.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
